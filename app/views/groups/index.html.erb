<h1>人生不順，就發廢文</h1>

<div class='col-md-12'>
  <div class='group'>
    <%= link_to "我要發廢文", new_group_path, class: 'btn btn-primary pull-right' %>
  </div>
  <table class="table table-hover">
    <thead>
      <tr>
        <td>#</td>
        <td>主題</td>
        <td>內容</td>
      </tr>
    </thead>
    <tbody>
      <% @groups.each do |group| %>
        <tr>
          <td> <%=group.id%> </td>
          <td><%= link_to truncate(group.title, length: 15), group_path(group) %></td>
          <td><%= group.description %></td>
          <td>
            <% if group.editable_by?(current_user) %>
              <%= link_to "編輯", edit_group_path(group), class:'btn btn-sm btn-default' %>
              <%= link_to "刪除", group_path(group), class:'btn btn-sm btn-default' ,
                      method: :delete, data:{confirm: '你真心要刪？'} %>
            <% else %>
              <p> 這不是你寫的，你不能改喲(^.<) </p>
            <% end %>
          </td>
        </tr>
      <% end%>
    </tbody>
  </table>
</div>




</div>
